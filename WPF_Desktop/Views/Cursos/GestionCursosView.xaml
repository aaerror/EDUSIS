<UserControl x:Class="WPF_Desktop.Views.Cursos.GestionCursosView"
             xmlns:local="clr-namespace:WPF_Desktop.Views.Cursos"
             xmlns:components="clr-namespace:WPF_Desktop.Components"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TITTLE AND SUBTITLE -->
        <components:Header Grid.Row="0"
                           Title="Gestión de Cursos"
                           Subtitle="Divisiones, Materias y Horarios"/>

        <!-- INPUTS -->
        <WrapPanel Grid.Row="1"
                   Margin="26 10">
            <Button Command="{ Binding NuevoCursoCommand }"
                    Content="Nuevo Curso"
                    Margin="0 0 5 0"
                    Style="{ StaticResource Secondary }"/>
            <Button Command="{ Binding NuevaDivisionCommand }"
                    Content="Nueva Division"
                    Margin="5 0 5 0"
                    Style="{ StaticResource Secondary }"/>
            <Button Command="{ Binding NuevaMateriaCommand }"
                    Content="Diseño Curricular"
                    Margin="5 0 0 0"
                    Style="{ StaticResource Secondary }"/>
        </WrapPanel>

        <!-- DETAILS -->
        <Border Grid.Row="2"
                Background="#FFFFFF"
                BorderBrush="{ StaticResource Gray200 }"
                BorderThickness="1"
                CornerRadius="4"
                Margin="20 10"
                Padding="10 10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15"
                                  Color="#373737"
                                  ShadowDepth="5"
                                  Opacity=".3"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <WrapPanel Grid.Row="0"
                           HorizontalAlignment="Left"
                           Margin="0 10">
                    <Button Command="{ Binding MostrarCursosCommand }"
                            Content="Mostrar Cursos"
                            Margin="0"
                            Style="{ StaticResource Primary }"/>
                </WrapPanel>

                <DataGrid Grid.Row="1"
                          AutoGenerateColumns="False"
                          ItemsSource="{ Binding Path=Cursos, UpdateSourceTrigger=PropertyChanged }"
                          SelectedItem="{ Binding Path=Curso, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay }"
                          SelectionMode="Extended"
                          SelectionUnit="FullRow"
                          Style="{ DynamicResource DataGrid }">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="CursoID"
                                            Binding="{ Binding CursoId }"
                                            Visibility="Hidden"
                                            Width="Auto"/>
                        <DataGridTextColumn Header="Curso"
                                            Binding="{ Binding Descripcion }"
                                            Width="Auto"/>
                        <DataGridTextColumn Header="Nivel Educativo"
                                            Binding="{ Binding NivelEducativo }"
                                            Width="Auto"/>
                        <DataGridTextColumn Header="Cant. Divisiones"
                                            Binding="{ Binding Divisiones }"
                                            Width="Auto"/>
                        <DataGridTextColumn Header="Cant. Materias"
                                            Binding="{ Binding Materias }"
                                            Width="Auto"/>
                        <DataGridTextColumn Header="Cant. Alumnos"
                                            Binding="{ Binding Alumnos }"
                                            Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
